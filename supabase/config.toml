project_id = "yntpprkrsuwiaogccirh"

[functions.generate-shopify-app]
verify_jwt = false

# OAuth callback is hit by Shopify's redirect (no JWT); auth is enforced via
# HMAC + a CSRF state nonce inside the function.
[functions.shopify-oauth]
verify_jwt = false

[functions.generate-web-app]
verify_jwt = false

[functions.generate-app-preview]
verify_jwt = false

# Stripe calls this with its own signature (no Supabase JWT); verified via
# STRIPE_WEBHOOK_SECRET inside the function.
[functions.stripe-webhook]
verify_jwt = false
